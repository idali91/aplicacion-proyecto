apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: reverse-words-git
  namespace: reverse-words-ci
spec:
  type: git
  params:
  - name: url
    value: https://github.com/idali91/aplicacion-proyecto
  - name: revision
    value: main
  - name: sslVerify
    value: "false"
---
apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: reverse-words-image
  namespace: reverse-words-ci
spec:
  type: image
  params:
  - name: url
    value: quay.io/idali91/aplicacion-proyecto
---
apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: reverse-words-build-pipeline
  namespace: reverse-words-ci
spec:
  resources:
  - name: app-git
    type: git
  - name: app-image
    type: image
  params:
  - name: imageTag
    type: string
    default: latest
    description: Tag for the built image
  tasks:

  - name: lint
    taskRef:
      name: golangci-lint
    params:
      - name: package
        value: github.com/idali91/aplicacion-proyecto
      - name: flags
        value: --verbose
      - name: GOOS
        value: "linux"
    resources:
      inputs:
      - name: source
        resource: app-git

  - name: test
    taskRef:
      name: golang-test
    runAfter:
      - lint
    params:
      - name: package
        value: github.com/idali91/aplicacion-proyecto
      - name: GOOS
        value: "linux"
    resources:
      inputs:
      - name: source
        resource: app-git

  - name: build-and-push
    taskRef:
      name: build-from-dockerfile
    runAfter:
      - test
    params:
      - name: DOCKERFILE_PATH
        value: "./Dockerfile"
      - name: IMAGE_TAG
        value: "$(params.imageTag)"
    resources:
      inputs:
      - name: source
        resource: app-git
      outputs:
      - name: image
        resource: app-image

  - name: update-stage-deployment-image
    taskRef:
      name: image-updater
    runAfter:
      - build-and-push
    params:
      - name: driver
        value: "github"
      - name: file-path
        value: "deployment.yaml"
      - name: image-repo
        value: "quay.io/idali91/aplicacion-proyecto"
      - name: new-image-url
        value: "quay.io/idali91/aplicacion-proyecto:$(params.imageTag)"
      - name: source-branch
        value: "desarrollo"
      - name: source-repo
        value: "idali91/aplicacion-proyecto"
      - name: update-key
        value: "spec.template.spec.containers.0.image"
      - name: insecure
        value: "true"
      - name: branch-generate-name
        value: ""
